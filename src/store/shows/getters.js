export default {
  shows(state) {
    return state.shows;
  },

  numberOfPagesShows(state) {
    return state.numberOfPagesShows;
  },

  searchedShows(state) {
    return state.searchedShows;
  },

  currentShow(state) {
    return state.currentShow;
  },
};
